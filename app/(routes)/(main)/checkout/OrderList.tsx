'use client'
import { useCartQuery } from '@/app/_hooks/useCart'

export default function OrderItemList() {
	/* const { cartForCheckoutQuery } = useCartQuery()

	const { data } = cartForCheckoutQuery

	console.log(data) */

	return <div>Order List</div>
}
