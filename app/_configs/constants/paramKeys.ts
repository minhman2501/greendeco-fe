export enum SortBy {
	CreatedAt = 'created_at',
	Price = 'price',
	Star = 'star',
}

export enum Sort {
	Ascending = 'asc',
	Descending = 'desc',
}

export enum OrderState {
	Draft = 'draft',
	Processing = 'processing',
	Completed = 'completed',
	Cancelled = 'cancelled',
}
